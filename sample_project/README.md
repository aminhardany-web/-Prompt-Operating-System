# PKEA Sample Project

This is the canonical smoke-test project for the Project Knowledge & Evidence Agent.

Run:

```bash
python -m pkea.cli run ./sample_project --output ./workspace
```

The expected artifacts are `document_registry.json`, `analysis.json`, `report.md`, and `run_summary.json`.
