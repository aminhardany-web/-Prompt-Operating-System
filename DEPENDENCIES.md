# Dependency Registry

## Purpose
Document relationships between repository components.

## Core Flow
Architecture -> Governance -> Lifecycle -> Dependency Management -> Registry -> Testing -> Execution Records

## Rule
Dependencies must be recorded before new components are introduced.
