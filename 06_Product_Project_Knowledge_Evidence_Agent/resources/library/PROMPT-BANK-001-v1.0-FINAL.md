# PROMPT-BANK-001 — FINAL

## Prompt Bank Governance & Operational Protocol

**Version:** v1.0 FINAL  
**Status:** FROZEN  
**Mode:** ACTIVE  
**Lock:** TRUE  
**Scope:** PROMPT BANK PROJECT ONLY  
**Operational:** TRUE

---

## 1. اصل ثبت

هر پرامپت مستقل باید با متن کامل، عیناً و بدون هیچ تغییر در رکورد ثبت شود.

ممنوع است:
- خلاصه‌سازی
- حذف
- بازنویسی
- اصلاح خاموش
- تغییر ترتیب
- تغییر زبان یا معنا
- ادغام پرامپت‌های مستقل
- ثبت صرف شناسنامه بدون متن کامل
- جایگزین‌کردن متن اصلی با خلاصه

---

## 2. تشخیص پرامپت

متن زمانی به‌عنوان Prompt ثبت می‌شود که حداقل یکی از عناصر زیر را داشته باشد:
- نقش
- مأموریت
- دستور اجرایی
- هدف
- ورودی
- خروجی
- فرآیند
- محدودیت
- قاعده رفتاری
- الزام قالب خروجی

موارد غیرپرامپتی، مگر دارای دستور اجرایی مستقل:
- مقاله
- آموزش
- توضیح معمولی
- پاسخ مدل
- گزارش وضعیت
- لینک یا منبع
- نمونه خروجی
- شناسنامه پروژه
- رکورد داده
- قالب خالی

---

## 3. تفکیک نوع قلم

هر ورودی ابتدا به یکی از انواع زیر تفکیک می‌شود:
- PROMPT
- DIRECTIVE
- TEMPLATE
- STRUCTURED RECORD
- MODEL RESPONSE
- SOURCE / REFERENCE
- OTHER

پاسخ مدل، شناسنامه یا رکورد داده بدون دستور اجرایی مستقل، Prompt محسوب نمی‌شود.

---

## 4. گردش‌کار

```text
Detect
→ Separate
→ Extract
→ Preserve Verbatim
→ Classify
→ Assign ID
→ Profile
→ Score
→ Audit
→ Compare
→ Decide
→ Retrieve
→ Index
→ Lock
```

---

## 5. شناسه

هر پرامپت مستقل شناسه یکتا با الگوی زیر دریافت می‌کند:

`PB-XXX`

اگر متن دقیقاً مشابه رکورد موجود باشد، رکورد جدید ساخته نمی‌شود و شناسه موجود حفظ می‌شود.

اگر متن یا نسخه متفاوت باشد، رکورد جدید با شناسه جدید ایجاد می‌شود.

---

## 6. حفظ متن اصلی

`PROMPT CONTENT` تنها محل متن اصلی Prompt است.

متن باید:
- کامل باشد.
- عیناً حفظ شود.
- بدون تفسیر ثبت شود.
- بدون اصلاح ثبت شود.
- بدون ترکیب با Metadata ثبت شود.

ممیزی و تحلیل هرگز نباید متن منبع را تغییر دهد.

---

## 7. پروفایل

اطلاعات تحلیلی خارج از متن اصلی ثبت می‌شود:
- Mission
- Goal
- Usage Type
- Category
- Complexity
- Source
- Lifecycle Status

---

## 8. امتیاز مهندسی

هر Prompt بر اساس پنج معیار ارزیابی می‌شود:
1. Clarity /20
2. Structure /20
3. Executability /20
4. Constraint & Error Control /20
5. Reusability /20

امتیاز کل: `XX/100`

---

## 9. ممیزی فنی

برای هر Prompt یک ممیزی کوتاه و مستند ثبت می‌شود.

ممیزی باید مشخص کند:
- نوع کاربری واقعی چیست.
- خروجی مورد انتظار حرفه‌ای است یا سطحی.
- ضعف فنی اصلی چیست.
- منطق اجرایی کافی است یا خیر.
- محدودیت‌ها و کنترل خطا مناسب هستند یا خیر.
- آیا نسخه بهتر یا جامع‌تر در بانک وجود دارد یا خیر.

ممیزی نباید نمایشی، کلیشه‌ای یا صرفاً توصیفی باشد.

---

## 10. مقایسه با نسخه‌های موجود

برای هر Prompt بررسی شود:
- Similar Version
- Better Version
- Canonical Version

اگر نسخه بهتر در بانک وجود دارد، شناسه آن ثبت شود.

هیچ نسخه جایگزینی بدون مبنای مشخص و قابل ردیابی اعلام نمی‌شود.

---

## 11. تصمیم نهایی

یکی از وضعیت‌های زیر ثبت می‌شود:
- KEEP
- KEEP WITH WARNING
- REPLACE
- SPECIALIZED-ONLY
- REJECT

تصمیم باید بر اساس ممیزی فنی و مقایسه با رکوردهای موجود باشد.

---

## 12. پروتکل بازیابی

هر Prompt باید حداقل از یکی از مسیرهای زیر قابل بازیابی باشد:
- PROMPT ID
- TITLE
- CATEGORY
- USAGE TYPE
- KEYWORDS

مسیر استاندارد:

`Prompt Bank / Category / PB-ID`

---

## 13. کنترل تکرار

اگر متن و نسخه دقیقاً مشابه رکورد موجود باشد:

`NO NEW RECORD`

اگر متن یا نسخه متفاوت باشد:

`NEW RECORD`

نسخه قبلی حذف نمی‌شود.

---

## 14. قالب استاندارد رکورد

هر رکورد شامل شناسه، عنوان، دسته، وضعیت، نسخه، متن کامل، پروفایل، امتیاز مهندسی، ممیزی، تصمیم، مسیر بازیابی، رکورد استخراج و شاخص است.

---

## 15. تفکیک ثبت از تأیید

قاعده قطعی:

`ثبت در بانک ≠ تأیید مفاد`

`ثبت در بانک ≠ اجرای موفق`

`ثبت در بانک ≠ Validation`

`ثبت در بانک ≠ Canonical بودن`

ثبت فقط به معنای ورود کنترل‌شده Prompt به Repository است.

---

## 16. کنترل نسخه

هر نسخه معتبر باید مستقل قابل ردیابی باشد.

نسخه جدید:
- شناسه جدید می‌گیرد، مگر صرفاً اصلاح Metadata باشد.
- نسخه قبلی را حذف نمی‌کند.
- رابطه با نسخه قبلی ثبت می‌شود.
- دلیل تغییر ثبت می‌شود.

---

## 17. محدوده اجرا

این پروتکل فقط متعلق به **PROMPT BANK PROJECT** است.

این پروتکل الگوی عمومی پاسخ‌گویی نیست، روی پروژه‌های دیگر اعمال نمی‌شود و بدون ارجاع به بانک پرامپت فعال نمی‌شود.

---

## 18. وضعیت نهایی پروتکل

```text
PROMPT-BANK-001-v1.0-FINAL
PROTOCOL: FROZEN
MODE: ACTIVE
SCOPE: PROMPT BANK PROJECT ONLY
VERBATIM: REQUIRED
FULL-TEXT: REQUIRED
STRICT IDENTIFICATION: ON
VERSION PRESERVATION: ON
AUDIT: ON
BETTER-VERSION DETECTION: ON
REPLACEMENT / REJECTION CONTROL: ON
RETRIEVAL: ON
INDEXING: ON
LOCK: TRUE
OPERATIONAL: TRUE
STATUS: FINAL
```

---

## 19. قاعده حاکم نهایی

متن اصلی Prompt همیشه از Metadata، ممیزی و تصمیم اجرایی مستقل نگهداری می‌شود.

هیچ ممیزی، امتیازدهی، مقایسه، Canonicalization یا تصمیم جایگزینی مجاز نیست که متن منبع را بی‌ردپا تغییر دهد.

**PROMPT-BANK-001-v1.0-FINAL** مرجع اجرایی پروتکل داخلی بانک پرامپت است.
